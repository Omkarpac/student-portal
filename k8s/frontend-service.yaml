apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: NodePort              # reachable from outside the cluster for local testing
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080         # the external port on the node (must be 30000–32767)